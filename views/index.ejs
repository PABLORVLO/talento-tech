<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #111; /* Cambio de color de fondo */
      margin: 0;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #fff; /* Cambio de color del texto */
    }

    p {
      margin-bottom: 10px;
      color: #fff; /* Cambio de color del texto */
    }

    ul {
      list-style-type: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-around;
    }

    li {
      background-color: #222; /* Cambio de color de fondo */
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.3); /* Ajuste de sombra */
      margin: 10px;
      padding: 20px;
      width: 45%;
      max-width: 300px;
      transition: transform 0.3s ease-in-out; /* Transición para el efecto de hover */
    }

    li:hover {
      transform: translateY(-5px) scale(1.05); /* Efecto de hover */
    }

    img {
      display: block;
      margin: 0 auto 10px;
      max-width: 100%;
      border-radius: 5px;
    }

    h3 {
      color: #fff; /* Cambio de color del texto */
      font-size: 1.2em;
      margin-bottom: 10px;
    }

    p.book-details {
      color: #bbb; /* Cambio de color del texto */
      font-size: 0.9em;
      margin-top: 10px;
    }

    .buy-button {
      background-color: #4CAF50;
      border: none;
      color: white;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .buy-button:hover {
      background-color: #45a049;
    }
  </style>
</head>
<body>
  <h1><%= title %></h1>
  <p>Bienvenido <%= mensaje %></p>
  <br>

  <ul>
    <% books.forEach((libro) => { %>
      <li>
        <img src="images/<%= libro.cover %>" alt="<%= libro.title %>_cover">
        <h3><%= libro.title %> by <%= libro.author %></h3>
        <p class="book-details">
          Año: <%= libro.year %><br>
          Publicación: <%= libro.published %><br>
          Cantidad: <%= libro.quantity %><br>
          Género: <%= libro.genre %><br>
          Precio: <%= libro.price %><br>
          Sinopsis: <%= libro.synopsis %>
        </p>
        <button class="buy-button">$<%= libro.price %> Comprar</button> <!-- Botón de compra -->
      </li>
    <% }) %>
  </ul>
</body>
</html>



